# Upload directory path (defaults to ./uploads relative to project root in dev,
# /app/uploads in Docker via volume mount)
UPLOAD_DIR=

# Max upload size per file in bytes (default: 10MB = 10485760)
MAX_FILE_SIZE=10485760
